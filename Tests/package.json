{
  "type": "module",
  "scripts": {
    "watchTS": "dotnet fable watch --outDir ts --lang typescript ../Src/Str.fsproj --run npx tsc" ,

    "buildTS": "dotnet fable --outDir ts --noCache  --lang typescript ../Src/Str.fsproj --silent --run npx tsc" ,
    "pretest": "dotnet fable --outDir js --noCache ",
    "test": "mocha js && npm run buildTS"
  },
  "dependencies": {
    "mocha": "^11.5.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
